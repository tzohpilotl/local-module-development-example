{
  "name": "client",
  "version": "1.0.0",
  "scripts": {
    "develop": "nodemon --watch index.js --watch ~/.yalc/ --exec \"./reload\""
  },
  "dependencies": {
    "library-bundle": "file:.yalc/library-bundle"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
