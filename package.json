{
  "name": "library-bundle",
  "version": "1.0.0",
  "description": "Testing packing a library with webpack",
  "main": "src/index.js",
  "scripts": {
    "build": "webpack",
    "develop": "concurrently \"webpack --watch\" \"nodemon --watch dist --exec ./publish \"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.0.0",
    "nodemon": "^2.0.0",
    "prettier": "^1.19.1"
  }
}
